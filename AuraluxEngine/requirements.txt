# AuraluxEngine dependencies
#
# All Python dependencies are managed by ACE-Step 1.5's pyproject.toml
# via the `uv` package manager.  Run `./setup_env.sh` to install them.
#
# The setup process:
#   1. Clones ACE-Step 1.5 into ./ACE-Step-1.5/
#   2. Runs `uv sync` which reads ACE-Step-1.5/pyproject.toml
#   3. Creates a venv at ./ACE-Step-1.5/.venv/
#
# Key macOS Apple Silicon (arm64) dependencies installed:
#
#   torch>=2.9.1              PyTorch with MPS backend
#   torchaudio>=2.9.1         Audio I/O
#   mlx>=0.25.2               Apple Silicon native ML (for LM backend)
#   mlx-lm>=0.20.0            MLX language model support
#   transformers>=4.51.0      HuggingFace transformers
#   diffusers                 Diffusion model utilities
#   soundfile>=0.13.1         Audio file I/O
#   scipy>=1.10.1             Scientific computing
#   einops>=0.8.1             Tensor operations
#   accelerate>=1.12.0        HuggingFace Accelerate
#   huggingface-hub           Model downloading
#   loguru>=0.7.3             Logging
#
# To install manually (not recommended, use setup_env.sh instead):
#   cd ACE-Step-1.5 && uv sync
